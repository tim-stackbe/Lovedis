{
  "install": "npm ci && npx prisma generate && sudo apt-get update -qq && sudo apt-get install -y -qq rsync openssh-client",
  "start": "if [ -n \"${SSH_KEY:-}\" ]; then echo 'SSH_KEY configured for Hetzner deploy'; else echo 'SSH_KEY not set — add deploy private key to environment secrets'; fi"
}
