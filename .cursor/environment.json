{
  "install": "npm ci && npx prisma generate",
  "start": "echo 'Hetzner deploy: run ./deploy/hetzner/deploy-platform.sh from your Mac (local SSH). Cloud agents must not attempt Hetzner SSH unless SSH_KEY is configured in environment secrets.'"
}
